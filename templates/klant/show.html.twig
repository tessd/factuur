{% extends 'base.html.twig' %}

{% block title %}Klant{% endblock %}

{% block body %}
    <h1>Klant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ klant.id }}</td>
            </tr>
            <tr>
                <th>Klantnr</th>
                <td>{{ klant.klantnr }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ klant.naam }}</td>
            </tr>
            <tr>
                <th>Btwnr</th>
                <td>{{ klant.btwnr }}</td>
            </tr>
            <tr>
                <th>Adres</th>
                <td>{{ klant.adres }}</td>
            </tr>
            <tr>
                <th>Postcode</th>
                <td>{{ klant.postcode }}</td>
            </tr>
            <tr>
                <th>Plaats</th>
                <td>{{ klant.plaats }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('klant_index') }}">back to list</a>

    <a href="{{ path('klant_edit', {'id': klant.id}) }}">edit</a>

    {{ include('klant/_delete_form.html.twig') }}
{% endblock %}
